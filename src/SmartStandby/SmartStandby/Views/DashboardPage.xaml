<Page
    x:Class="SmartStandby.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmartStandby.Views"
    xmlns:models="using:SmartStandby.Core.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:SmartStandby.Converters"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.Resources>
            <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="24,24,24,12" Orientation="Vertical">
            <TextBlock Text="Dashboard" Style="{ThemeResource TitleTextBlockStyle}"/>
            <TextBlock Text="{Binding StatusMessage}" Foreground="Gray" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Blocker List -->
        <ListView Grid.Row="1" ItemsSource="{Binding Blockers}" Margin="24,0,24,12" BorderThickness="1" BorderBrush="#33000000" CornerRadius="4">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:BlockerInfo">
                    <StackPanel Orientation="Vertical" Padding="12">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Border Background="#FF0078D7" CornerRadius="2" Padding="4,0" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind Type}" Foreground="White" FontSize="10" FontWeight="SemiBold"/>
                            </Border>
                            <TextBlock Text="{x:Bind Name}" FontWeight="Bold"/>
                        </StackPanel>
                        <TextBlock Text="{x:Bind RawDetails}" FontSize="11" Foreground="Gray" Margin="0,4,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Margin="24,12,24,24" Orientation="Horizontal" Spacing="12" HorizontalAlignment="Center">
            <Button Command="{Binding RefreshBlockersCommand}" Content="Refresh Blockers" Width="120" Height="40"/>
            <Button Command="{Binding SmartSleepCommand}" Content="SMART SLEEP" Style="{StaticResource AccentButtonStyle}" Width="160" Height="40" Background="#FF0078D7" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
        </StackPanel>
        
        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="3" Background="#88000000" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
            <ProgressRing IsActive="True" Width="64" Height="64" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Page>
